# Prompt 1: Doc de arquitetura Night Worker

Crie o documento docs/NIGHTWORKER_ARCHITECTURE.md com a arquitetura completa do Night Worker: visão geral (Frontend → API FastAPI → Vendors → LLMs), fluxo de dados, 3 opções de orquestração (Orchestrator central, PM2/Systemd, Híbrido), estrutura de arquivos em claude-auto, como funciona hoje passo a passo, pontos de melhoria, dependências e roadmap. No topo, deixe explícito que o Frontend é este repo (minimal-idea-spark) e que API/Vendors são o projeto externo (claude-auto). No final, adicione a seção "Score / O que faz sentido" com uma tabela classificando cada item em Alta/Média/Baixa/N/A e uma nota curta.

---
Fim do prompt
